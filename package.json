{
  "name": "file-city-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build:builder && bun run build:react && bun run build:server && bun run build:cli",
    "build:builder": "cd packages/builder && bun run build",
    "build:react": "cd packages/react && bun run build",
    "build:server": "cd packages/server && bun run build",
    "build:cli": "cd packages/cli && bun run build",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "eslint . --ext .ts,.tsx,.js,.jsx --fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "publish:all": "bun run publish:builder && bun run publish:react && bun run publish:server && bun run publish:cli",
    "publish:builder": "cd packages/builder && npm publish --access public",
    "publish:react": "cd packages/react && npm publish --access public",
    "publish:server": "cd packages/server && npm publish --access public",
    "publish:cli": "cd packages/cli && npm publish --access public",
    "version:sync": "bun run version:set",
    "version:set": "echo 'Setting version...' && bun scripts/sync-versions.js",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.2",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^4.6.2",
    "husky": "^9.1.7",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "packageManager": "bun@1.2.5",
  "version": "0.0.1"
}

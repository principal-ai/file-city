export interface GitHubRepositoryInfo {
    /** GitHub repository owner/organization */
    owner: string;
    /** Repository name */
    repo: string;
    /** Branch/ref to use for raw URLs (defaults to 'main' if not specified) */
    branch?: string;
    /** Base path within the repository (for files in subdirectories) */
    basePath?: string;
}
/**
 * Parses a GitHub URL and extracts repository information
 */
export declare function parseGitHubUrl(url: string): GitHubRepositoryInfo | null;
//# sourceMappingURL=github-tools.d.ts.map
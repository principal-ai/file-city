"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseGitHubUrl = parseGitHubUrl;
/**
 * Parses a GitHub URL and extracts repository information
 */
function parseGitHubUrl(url) {
    try {
        // Handle various GitHub URL formats:
        // https://github.com/owner/repo
        // https://github.com/owner/repo/blob/branch/path/to/file.md
        // https://github.com/owner/repo/tree/branch/path/to/directory
        // https://raw.githubusercontent.com/owner/repo/branch/path/to/file.md
        const githubUrl = url.trim();
        // Handle raw.githubusercontent.com URLs
        const rawMatch = githubUrl.match(/^https?:\/\/raw\.githubusercontent\.com\/([^/]+)\/([^/]+)\/([^/]+)\/(.*)$/);
        if (rawMatch) {
            const [, owner, repo, branch, filePath] = rawMatch;
            return {
                owner,
                repo,
                branch,
                basePath: filePath.includes('/')
                    ? filePath.substring(0, filePath.lastIndexOf('/'))
                    : undefined,
            };
        }
        // Handle github.com URLs
        const githubMatch = githubUrl.match(/^https?:\/\/github\.com\/([^/]+)\/([^/]+)(?:\/(blob|tree)\/([^/]+)\/(.*))?$/);
        if (githubMatch) {
            const [, owner, repo, , branch = 'main', filePath = ''] = githubMatch;
            return {
                owner,
                repo,
                branch,
                basePath: filePath && filePath.includes('/')
                    ? filePath.substring(0, filePath.lastIndexOf('/'))
                    : undefined,
            };
        }
        // Handle owner/repo format
        const shortMatch = githubUrl.match(/^([^/]+)\/([^/]+)$/);
        if (shortMatch) {
            const [, owner, repo] = shortMatch;
            return {
                owner,
                repo,
                branch: 'main',
                basePath: undefined,
            };
        }
        return null;
    }
    catch {
        return null;
    }
}
//# sourceMappingURL=github-tools.js.map
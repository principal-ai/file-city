import { LibraryRule, LibraryLintResult, LibraryRuleSet } from './types';
import { AlexandriaConfig } from '../config/types';
export declare class LibraryRulesEngine {
    private rules;
    private configLoader;
    private fsAdapter;
    constructor();
    registerRule(rule: LibraryRule): void;
    getAllRules(): Map<string, LibraryRule>;
    private scanFiles;
    lint(projectRoot?: string, options?: {
        config?: AlexandriaConfig;
        enabledRules?: string[];
        disabledRules?: string[];
        fix?: boolean;
    }): Promise<LibraryLintResult>;
    getRuleSet(): LibraryRuleSet;
}
//# sourceMappingURL=engine.d.ts.map
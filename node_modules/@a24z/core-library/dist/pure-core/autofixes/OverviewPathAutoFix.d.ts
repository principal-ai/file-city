/**
 * Auto-fix provider for CodebaseView overview path issues
 * Handles moving misplaced overview documents and updating paths
 */
import type { MemoryPalace } from '../../MemoryPalace';
import { AutoFixProvider, AutoFixSuggestion, AutoFixResult } from './types';
export interface OverviewPathAutoFixOptions {
    /** Preferred directory for overview documents */
    preferredOverviewDir?: string;
    /** Patterns to exclude from auto-fix */
    excludePatterns?: string[];
    /** Whether to create missing overview files */
    createMissing?: boolean;
    /** Whether to consolidate docs to preferred directory */
    consolidateDocs?: boolean;
}
export declare class OverviewPathAutoFix implements AutoFixProvider {
    name: string;
    description: string;
    private palace;
    private options;
    constructor(palace: MemoryPalace, options?: OverviewPathAutoFixOptions);
    analyze(): Promise<AutoFixSuggestion[]>;
    private checkMissingOverview;
    private checkMisplacedOverview;
    private checkExcludedOverview;
    private checkOrphanedMarkdownFiles;
    private createMissingOverview;
    private moveOverviewFile;
    private generateOverviewContent;
    private generateCellsDescription;
    private generateLinksDescription;
    applyFix(suggestion: AutoFixSuggestion): Promise<AutoFixResult>;
    applyAllSafe(): Promise<AutoFixResult[]>;
}
//# sourceMappingURL=OverviewPathAutoFix.d.ts.map
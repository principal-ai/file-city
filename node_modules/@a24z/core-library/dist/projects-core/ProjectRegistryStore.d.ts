/**
 * Pure ProjectRegistryStore - Platform-agnostic project registry management
 *
 * Manages a registry of local project paths with their git remotes
 */
import { FileSystemAdapter } from '../pure-core/abstractions/filesystem';
import { ValidatedRepositoryPath } from '../pure-core/types';
import { AlexandriaEntry } from '../pure-core/types/repository';
export declare class ProjectRegistryStore {
    private fs;
    private registryPath;
    constructor(fileSystemAdapter: FileSystemAdapter, homeDir: string);
    /**
     * Ensure the registry directory exists
     */
    private ensureRegistryDir;
    /**
     * Load the project registry
     */
    private loadRegistry;
    /**
     * Save the project registry
     */
    private saveRegistry;
    /**
     * Register a new project
     */
    registerProject(name: string, projectPath: ValidatedRepositoryPath, remoteUrl?: string): void;
    /**
     * List all registered projects
     */
    listProjects(): AlexandriaEntry[];
    /**
     * Get a project by name
     */
    getProject(name: string): AlexandriaEntry | undefined;
    /**
     * Remove a project from the registry
     */
    removeProject(name: string): boolean;
    /**
     * Update a project's path or remote URL
     */
    updateProject(name: string, updates: Partial<Omit<AlexandriaEntry, 'name' | 'registeredAt'>>): void;
}
//# sourceMappingURL=ProjectRegistryStore.d.ts.map
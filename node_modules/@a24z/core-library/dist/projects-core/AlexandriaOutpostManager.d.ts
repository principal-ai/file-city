import type { AlexandriaRepository } from '../pure-core/types/repository.js';
import { FileSystemAdapter } from '../pure-core/abstractions/filesystem.js';
export declare class AlexandriaOutpostManager {
    private readonly fsAdapter;
    private readonly projectRegistry;
    constructor(fsAdapter: FileSystemAdapter);
    getAllRepositories(): Promise<AlexandriaRepository[]>;
    getRepository(name: string): Promise<AlexandriaRepository | null>;
    registerRepository(name: string, path: string): Promise<AlexandriaRepository>;
    getRepositoryByPath(path: string): Promise<AlexandriaRepository | null>;
    getRepositoryCount(): number;
    private transformToRepository;
    private extractOwner;
}
//# sourceMappingURL=AlexandriaOutpostManager.d.ts.map